# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Node / build
node_modules/
*.tsbuildinfo
src/**/__tests__/*.browser.bundle.js
src/**/__tests__/harness-style.css

# The built bundle at cairn_ui/_dist IS COMMITTED — it is the wheel's payload,
# so installing never needs Node. It deliberately does not match the usual
# `dist/` rule, which is why it is named with a leading underscore.
